name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout kalles-finance
      uses: actions/checkout@v4
      with:
        path: kalles-finance

    - name: Checkout kalles-traffic (for pubsub client)
      uses: actions/checkout@v4
      with:
        repository: fmanh3/kalles-traffic
        path: kalles-traffic
        # Assumes the repo is public or the runner has access

    - name: Use Node.js 22.x
      uses: actions/setup-node@v4
      with:
        node-version: '22.x'

    - name: Install dependencies
      run: npm ci
      working-directory: ./kalles-finance

    - name: Build TypeScript
      run: npm run build
      working-directory: ./kalles-finance

    # Kalles Finance har inga riktiga tester än, så vi kommenterar ut npm test för att inte faila bygget
    # - name: Run tests
    #   run: npm test
